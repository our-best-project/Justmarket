# 機密
.env
.env.*
!.env.example
# .env 的手動備份／舊版：只寫 .env 擋不到被改名的 env-old、env-bak，
# 但它們一樣含真實連線字串。不要寫成 env* —— 那會連 .env.example 一起擋掉。
env-*
*.pem
*.key

# 編輯器與 OS
.DS_Store
.idea/
.vscode/

# ── 個人／機器層設定，一律不進版控 ──────────────────────────────────────
# 這是給人與 agent 用的工作環境，不是產品的一部分；而且 .claude/skills 是指向
# 本機技能包的 symlink，推上去對別人只是壞掉的連結。
.claude/
.agents/
# LICENSES/ 只放 .agents/ 底下 vendored skills 的授權全文，跟著一起不進版控
LICENSES/
AGENTS.md
CLAUDE.md
CLAUDE.local.md
AGENTS.local.md

# 跨 session 記憶（機器層本機資料）
.memsearch/

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
dist/
build/
*.egg-info/

# 管線產物（可由 python -m backend pipeline 重建）
backend/clustering/out/
backend/embedding/out_subset/
# 註：embedding/out/ 刻意納入版控——embeddings.npy 重算需 CPU 40 分鐘，
# 提交後協作者 clone 即可直接跑聚類。

# 資料快取
data/*.db

# 暫存備份
**/*.bak

# 前端
**/node_modules/
**/dist/
web/test-results/
web/playwright-report/

# Spider Forge 執行期產物（docker-compose 的 bind mount 目標，內容不進版控）
runtime/spider_forge/**
!runtime/spider_forge/README.md
!runtime/spider_forge/requests/
!runtime/spider_forge/requests/urls.example.txt

# agent 專用（不是產品文件），跟著 .agents/ 一起不進版控
docs/agents/
